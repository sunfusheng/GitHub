<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="@dimen/titlebar_view_height"
    android:background="@color/colorPrimary"
    android:minHeight="@dimen/titlebar_view_height">

    <LinearLayout
        android:id="@+id/left_area"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_centerVertical="true"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/left_img"
            style="@style/titlebar_img"
            android:src="@mipmap/ic_action_arrow_back"
            android:visibility="gone"
            tools:visibility="visible"/>

        <TextView
            android:id="@+id/left_text"
            style="@style/textView.singleLine.titlebar_title.left"
            android:text="返回"
            android:visibility="gone"
            tools:visibility="visible"/>
    </LinearLayout>

    <TextView
        android:id="@+id/title"
        style="@style/textView.singleLine.titlebar_title"
        android:layout_centerInParent="true"
        android:text="标题"
        android:visibility="gone"
        tools:visibility="visible"/>

    <LinearLayout
        android:id="@+id/right_area"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/right_img"
            style="@style/titlebar_img"
            android:src="@mipmap/ic_action_add"
            android:visibility="gone"
            tools:visibility="visible"/>

        <ImageView
            android:id="@+id/right_img2"
            style="@style/titlebar_img"
            android:src="@mipmap/ic_action_search"
            android:visibility="gone"
            tools:visibility="visible"/>

        <TextView
            android:id="@+id/right_text"
            style="@style/textView.singleLine.titlebar_title.right"
            android:text="保存"
            android:visibility="gone"
            tools:visibility="visible"/>
    </LinearLayout>

</RelativeLayout>